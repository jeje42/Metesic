import { Videos } from '../../../both/collections/videos.collection';
import { Video } from '../../../both/models/video.model';

export function insertFile() {
  var fileObject : string = 'merde' && 'merde2';
  console.log('fileObject : ' + fileObject);
}
